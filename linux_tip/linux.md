# TIL linux

## code snippet

```shell
# disk mount wokring process
$ su
$ fdisk -l
$ mount /dev/sdbx /mount/point
$ df -h
```

```shell
# view file size as Mb when type ls -l
ls -l --block-size=M
```
